import {css} from 'styled-components';

export const ManropeFont = css`
  font-family: 'Manrope', sans-serif;
`;

export const regularWeight = css`
  font-weight: 400;
`;

export const mediumWeight = css`
  font-weight: 500;
`;

export const boldWeight = css`
  font-weight: 700;
`;

// header-1 font
export const font_header_1_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 3rem;
  line-height: 3.125rem;
  letter-spacing: -1.44px;
`;

export const font_header_1_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 3rem;
  line-height: 3.125rem;
  letter-spacing: -1.44px;
`;

// header-2 font
export const font_header_2_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 2.5rem;
  line-height: 2.625rem;
  letter-spacing: -1.2px;
`;

export const font_header_2_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 2.5rem;
  line-height: 2.625rem;
  letter-spacing: -1.2px;
`;

// header-3 font
export const font_header_3_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 2.25rem;
  line-height: 2.375rem;
  letter-spacing: -0.72px;
`;

export const font_header_3_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 2.25rem;
  line-height: 2.375rem;
  letter-spacing: -0.72px;
`;

// header-4 font
export const font_header_4_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 1.75rem;
  line-height: 2.125rem;
  letter-spacing: -0.64px;
`;

export const font_header_4_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 1.75rem;
  line-height: 2.125rem;
  letter-spacing: -0.64px;
`;

// header-5 font
export const font_header_5_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 1.5rem;
  line-height: 1.625rem;
  letter-spacing: -0.24px;
`;

export const font_header_5_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 1.5rem;
  line-height: 1.625rem;
  letter-spacing: -0.24px;
`;

// header-6 font
export const font_header_6_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 1.25rem;
  line-height: 1.5rem;
  letter-spacing: -0.2px;
`;

export const font_header_6_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 1.25rem;
  line-height: 1.5rem;
  letter-spacing: -0.2px;
`;

// body-1 font
export const font_body_1_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 1.125rem;
  line-height: 1.375rem;
  letter-spacing: -0.09px;
`;

export const font_body_1_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 1.125rem;
  line-height: 1.375rem;
  letter-spacing: -0.09px;
`;

// body-2 font
export const font_body_2_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 1rem;
  line-height: 1.25rem;
  letter-spacing: -0.08px;
`;

export const font_body_2_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 1rem;
  line-height: 1.25rem;
  letter-spacing: -0.08px;
`;

// body-3 font
export const font_body_3_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 0.875rem;
  line-height: 1.125rem;
  letter-spacing: -0.07px;
`;

export const font_body_3_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 0.875rem;
  line-height: 1.125rem;
  letter-spacing: -0.07px;
`;

// body-4 font
export const font_body_4_reg = css`
  ${ManropeFont}
  ${regularWeight}
  font-size: 0.75rem;
  line-height: 1rem;
  letter-spacing: -0.06px;
`;

export const font_body_4_bold = css`
  ${ManropeFont}
  ${boldWeight}
  font-size: 0.75rem;
  line-height: 1rem;
  letter-spacing: -0.06px;
`;
